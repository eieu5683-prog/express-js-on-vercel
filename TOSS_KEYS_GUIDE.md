# 토스페이먼츠 키 설정 가이드

토스페이먼츠를 사용하기 위해서는 **클라이언트 키**와 **시크릿 키** 두 개가 모두 필요합니다.

## 🔑 토스페이먼츠 키 종류

### 1. 클라이언트 키 (Client Key)
- **용도**: 결제창을 열 때 사용
- **사용 위치**: 클라이언트 사이드 (브라우저)
- **형식**: 
  - 테스트: `test_ck_...`
  - 라이브: `live_ck_...`
- **환경 변수명**: `NEXT_PUBLIC_TOSS_CLIENT_KEY`
- **접두사 필요**: `NEXT_PUBLIC_` (클라이언트에서 접근 가능하도록)

### 2. 시크릿 키 (Secret Key)
- **용도**: 결제 검증, 취소, 환불 등 서버 사이드 작업
- **사용 위치**: 서버 사이드 (API 라우트)
- **형식**:
  - 테스트: `test_sk_...`
  - 라이브: `live_sk_...`
- **환경 변수명**: `TOSS_SECRET_KEY`
- **접두사 금지**: `NEXT_PUBLIC_` 없이 설정 (보안)

## ✅ Vercel 환경 변수 설정

### 라이브 환경 (Production)

**Settings → Environment Variables**에서 다음 두 개를 모두 추가하세요:

| Name | Value | 환경 | 설명 |
|------|-------|------|------|
| `NEXT_PUBLIC_TOSS_CLIENT_KEY` | `live_ck_실제_클라이언트_키` | Production | 결제창 열기용 |
| `TOSS_SECRET_KEY` | `live_sk_실제_시크릿_키` | Production | 결제 검증용 (서버 전용) |

### 테스트 환경 (개발 중)

| Name | Value | 환경 | 설명 |
|------|-------|------|------|
| `NEXT_PUBLIC_TOSS_CLIENT_KEY` | `test_ck_테스트_클라이언트_키` | Development | 결제창 열기용 |
| `TOSS_SECRET_KEY` | `test_sk_테스트_시크릿_키` | Development | 결제 검증용 (서버 전용) |

## 🔒 보안 주의사항

### ✅ 올바른 설정

```bash
# 클라이언트 키 (클라이언트에서 사용)
NEXT_PUBLIC_TOSS_CLIENT_KEY=live_ck_...

# 시크릿 키 (서버에서만 사용)
TOSS_SECRET_KEY=live_sk_...
```

### ❌ 잘못된 설정

```bash
# ❌ 시크릿 키에 NEXT_PUBLIC_ 접두사 붙임 (위험!)
NEXT_PUBLIC_TOSS_SECRET_KEY=live_sk_...

# ❌ 클라이언트 키에 접두사 없음 (작동 안 함)
TOSS_CLIENT_KEY=live_ck_...
```

## 📍 키 사용 위치

### 클라이언트 사이드 (브라우저)

```typescript
// src/components/TossPayment.tsx
const clientKey = process.env.NEXT_PUBLIC_TOSS_CLIENT_KEY;
// ✅ 클라이언트에서 접근 가능
```

### 서버 사이드 (API 라우트)

```typescript
// app/api/payment/verify/route.ts (예시)
const secretKey = process.env.TOSS_SECRET_KEY;
// ✅ 서버에서만 접근 가능, 클라이언트에 노출되지 않음
```

## 🎯 토스페이먼츠 개발자센터에서 키 발급

1. [토스페이먼츠 개발자센터](https://developers.tosspayments.com) 접속
2. 로그인 후 **내 프로젝트** 선택
3. **API 키** 메뉴에서 확인:
   - **클라이언트 키**: `live_ck_...` 또는 `test_ck_...`
   - **시크릿 키**: `live_sk_...` 또는 `test_sk_...`

## ✅ 체크리스트

배포 전 확인:

- [ ] `NEXT_PUBLIC_TOSS_CLIENT_KEY`에 라이브 클라이언트 키 설정 (`live_ck_...`)
- [ ] `TOSS_SECRET_KEY`에 라이브 시크릿 키 설정 (`live_sk_...`)
- [ ] `TOSS_SECRET_KEY`에 `NEXT_PUBLIC_` 접두사가 **없음**
- [ ] `NEXT_PUBLIC_TOSS_CLIENT_KEY`에 `NEXT_PUBLIC_` 접두사가 **있음**
- [ ] Production 환경에만 실제 라이브 키 설정
- [ ] 두 개의 키가 모두 환경 변수에 추가됨

## 🚨 자주 하는 실수

1. **시크릿 키에 `NEXT_PUBLIC_` 붙이기**
   - ❌ `NEXT_PUBLIC_TOSS_SECRET_KEY` (위험!)
   - ✅ `TOSS_SECRET_KEY`

2. **클라이언트 키에 접두사 없이 설정**
   - ❌ `TOSS_CLIENT_KEY` (작동 안 함)
   - ✅ `NEXT_PUBLIC_TOSS_CLIENT_KEY`

3. **한 개만 설정하고 배포**
   - ❌ 클라이언트 키만 설정
   - ❌ 시크릿 키만 설정
   - ✅ 두 개 모두 설정 필요!

## 참고 자료

- [토스페이먼츠 API 키 가이드](https://docs.tosspayments.com/guides/using-api/api-keys)
- [토스페이먼츠 개발자센터](https://developers.tosspayments.com)

